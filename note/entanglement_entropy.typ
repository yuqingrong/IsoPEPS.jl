
== make the state into canonical form
$sum_s (A_L^s)^dagger A_L^s = I$

$sum_s A_R^s (A_R^s)^dagger = I$

== get $rho$
left fixed point: $E(l) = l$, 

right fixed point:$ E(r) = r$

$"Tr"(l r) =1$

$rho = sqrt(l) dot r dot sqrt(l)$, $"Tr"(rho) =1$

== calculate

$S = -"Tr"(rho log(rho))$

== TODO
1. rewrite the infinite MPS entanglement entropy function, ensure it follow the steps above

2. for the mutiline MPS, contract the nearest legs to make it have the shape of MPS.
