# Engineering decision records

This directory keeps durable plans and design notes for changes to IsoPEPS.
They are implementation history, not user-facing package documentation.

The supported setup, usage, and paper-reproduction instructions live in the
repository-root `README.md` and `docs/reproducibility.md`. Local agent scratch
records under `docs/superpowers/` are intentionally ignored.
